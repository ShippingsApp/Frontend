export default class RouteFilter {
    constructor(startPoint, finishPoint, startDate, finishDate, weight, height, length, width) {
        this.startPoint = startPoint;
        this.finishPoint = finishPoint;
        this.startDate = startDate;
        this.finishDate = finishDate;
        this.weight = weight;
        this.height = height;
        this.length = length;
        this.width = width;
    }
}
